####

6 “要不要全项目都换成 MethodHandle？”
经验法则

库 / 框架层（RPC、ORM、序列化、表达式引擎）——是：高频 + 复杂访问权限，值得迁移。

业务代码层（控制器拼装 DTO，偶尔改值）——否：反射更直观，投入产出低。

在多数企业项目中，你只需要把公共且高频的反射热点，比如大批量 Bean 属性复制、JSON 映射、ORM 结果映射等，替换为 MethodHandle 缓存；对普通业务代码保持反射即可。